# No third-party dependencies — the exporter uses only the Python 3.11+ standard
# library (urllib, tomllib, argparse, datetime, pathlib, logging).
# Just run it with your system Python 3.11+ (you have 3.14).
