{
  "name": "trmnl-health",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-source health dashboard for TRMNL + daily Obsidian export. Ultrahuman Ring + Home now; Withings/Fitbit/Polar via the same source-adapter pattern.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/run.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
